# Core numerical / scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0

# Plotting
matplotlib>=3.7

# Graph algorithms (strongly connected components, etc.)
networkx>=3.1

# Parallelized replications in get_slope_average
joblib>=1.3

# Jupyter (only needed to open/run the .ipynb files themselves)
jupyter>=1.0
notebook>=7.0
